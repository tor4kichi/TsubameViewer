<UserControl
    x:Class="TsubameViewer.Presentation.Views.CardViewFolderListItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TsubameViewer.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:uwpToolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
  mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
  xmlns:vm="using:TsubameViewer.Presentation.ViewModels"
  d:DataContext="{d:DesignInstance Type=vm:StorageItemViewModel, IsDesignTimeCreatable=True}"
  >

  <Grid>
    <uwpToolkit:DockPanel>

      <Border uwpToolkit:DockPanel.Dock="Top">
        <Rectangle Fill="Red" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" />
      </Border>

      <TextBlock Text="{Binding Name}" />
    </uwpToolkit:DockPanel>

  </Grid>
</UserControl>
